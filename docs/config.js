/* ════════════════════════════════════════════════════════════════
   «Содержание» · КОНФИГ МИНИ-АППА
   Это ЕДИНСТВЕННЫЙ файл, который нужно править. Заполнили — готово.
   ════════════════════════════════════════════════════════════════ */

window.SZ_CONFIG = {

  /* ── 1. КАНАЛ (заполняет покупатель) ────────────────────────── */

  // Название канала, как оно будет в шапке оглавления:
  channel_name: "Оглавление · DemoTest",

  // Ссылка на канал (обязательно с https://):
  // пока поле пустое, кнопка «Канал» в шапке не показывается —
  // впишите ссылку, и кнопка появится сама:
  channel_url: "https://t.me/Testovuj",

  // Кнопка в шапке (можно написать «Подписаться»):
  channel_button: "Канал",

  // Эмодзи-логотип в шапке (можно любой свой):
  header_emoji: "📖",

  // Юзернейм бота этого мини-аппа (без @; можно оставить ""):
  // нужен для баннера-спасателя. Если приложение открыли по прямой
  // ссылке (мост Telegram недоступен), кнопка баннера ведёт на
  // t.me/<бот>?startapp — правильный вход в мини-апп. Оставите "" —
  // кнопка баннера откроет канал из поля channel_url выше.
  bot_username: "",

  // ── 2. ЛИЦЕНЗИЯ (заполняет ПРОДАВЕЦ при передаче набора) ─────

  // Имя покупателя, как в лицензии (водяной знак в футере):
  licensee: "@maxdrivero",

  // Лицензионный ключ, выданный продавцом (формат SZ-XXXXXXXXXXXXXXXX):
  license_key: "SZ-3390CDC041402A99",

  // Короткий номер лицензии для учёта (любая пометка, можно не менять):
  license_id: "2026-059",

  // ── Реферальная ссылка владельца (заполняется ботом автоматически) ──
  // Персональный код этого экземпляра: футер каталога становится ссылкой
  // «⚡ Каталог от Оглавление» → t.me/<бот>?start=r<ref_code>. Код работает
  // как приглашение: приведённый админ получает +7 дней триала, владелец — награду.
  ref_code: "",

  // Юзернейм бота для ссылки в футере (заполняется ботом автоматически):
  ref_bot: ""

  /* ── 3. Больше ничего менять не нужно ─────────────────────────
     Цвета темы можно поменять в index.html (блок :root) — это
     опционально; по умолчанию используется тёплая «бумажная» тема,
     которая автоматически переключается на тёмную вместе с Telegram.
     ────────────────────────────────────────────────────────────── */
};
